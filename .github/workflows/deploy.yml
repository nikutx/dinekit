name: Deploy to WordPress.org

# Push a version tag (e.g. `1.1.0`, created by `node bump.js 1.1.0`) to release.
# The tag name must equal the readme.txt "Stable tag" — bump.js keeps them in sync.
on:
  push:
    tags:
      - "*.*.*"

jobs:
  release:
    name: Release ${{ github.ref_name }}
    runs-on: ubuntu-latest
    # Needed for the optional "attach zip to GitHub Release" step to create a release.
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Build the plugin bundle
        run: |
          npm ci
          npm run build

      - name: Deploy to wordpress.org SVN
        id: deploy
        uses: 10up/action-wordpress-plugin-deploy@stable
        with:
          generate-zip: true
        env:
          SLUG: dinekit
          SVN_USERNAME: ${{ secrets.SVN_USERNAME }}
          SVN_PASSWORD: ${{ secrets.SVN_PASSWORD }}
          # ASSETS_DIR defaults to .wordpress-org (icon/banner/screenshots live there).
          # Trunk contents = repo minus .distignore, matching `npm run zip`.

      # Convenience only: attach the built zip to the GitHub Release. The wp.org
      # deploy above is the actual release, so never let a hiccup here fail the run.
      - name: Upload built zip to the GitHub Release
        if: ${{ github.event.repository.private == false }}
        continue-on-error: true
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.deploy.outputs.zip-path }}
